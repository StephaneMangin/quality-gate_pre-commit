#!/bin/bash
# Pre-commit runner - launcher minimal vers l'orchestrateur canonique

set -euo pipefail

GLOBAL_HOOKS_DIR="$HOME/.config/git/hooks"

HOOKS_PRECOMMIT="$GLOBAL_HOOKS_DIR/pre-commit"
exec "$HOOKS_PRECOMMIT" "$@"
